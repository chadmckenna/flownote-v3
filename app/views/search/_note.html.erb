<li>
  <%= link_to edit_folder_note_path(note.folder, note), data: { turbo_frame: "_top" } do %>
    <span class="file-listing__type">-</span>
    <span class="file-listing__name"><%= note.title %></span>
    <span class="file-listing__meta"><%= folder_path_string(note.folder) %></span>
  <% end %>
</li>
