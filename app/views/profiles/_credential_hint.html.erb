<%# Tells password managers which account these password fields belong to; browsers warn
    about a password form with no username field. Nameless so it isn't submitted, and
    inline display:none because forms.css sets `display: block` on inputs, beating [hidden]. %>
<input type="text" hidden readonly autocomplete="username" style="display: none"
       value="<%= @user.email_address %>">
